# Docker Configuration
DOCKER_HOST=
DOCKER_HOST_PLATFORM=linux/amd64
# MinIO (used when running via docker compose)
# API in Docker uses S3_ENDPOINT=http://minio:9000 (service name)
# API on host uses S3_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=local
MINIO_ROOT_PASSWORD=local123

# SendGrid Mock
SENDGRID_MOCK_API_KEY=SG.secret

# App Identity (for multi-app ecosystem)
APP_ID=dx3-local

# TimescaleDB (centralized logging) - Optional
# Set to 'true' to enable. Requires ax-infrastructure or standalone TimescaleDB.
# For integration mode, use: make dev-integration (reads from .env.integration)
TIMESCALE_ENABLED=false
TIMESCALE_URI=postgresql://axuser:axpassword@ax-timescaledb:5432/ax_logs
