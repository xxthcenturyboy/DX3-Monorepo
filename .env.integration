# ============================================================================
# Integration Mode Environment Variables
# ============================================================================
# This file configures dx3-monorepo to connect to ax-infrastructure services.
# Used with: make dev-integration
#
# Requires ax-infrastructure running:
#   cd ~/Developer/ArteFX/ax-infrastructure && make up
# ============================================================================

# App Identity
APP_ID=dx3-default

# ============================================================================
# Docker / MinIO / SendGrid (local services)
# ============================================================================
DOCKER_HOST=
DOCKER_HOST_PLATFORM=linux/amd64
MINIO_ROOT_USER=local
MINIO_ROOT_PASSWORD=local123
SENDGRID_MOCK_API_KEY=SG.secret

# ============================================================================
# Shared Infrastructure - TimescaleDB (Logging & Metrics)
# ============================================================================
TIMESCALE_ENABLED=true
TIMESCALE_URI=postgresql://axuser:axpassword@host.docker.internal:5434/ax_logs

# ============================================================================
# Shared Infrastructure - PostgreSQL (for lightweight apps)
# ============================================================================
# Note: dx3-monorepo typically uses its own PostgreSQL
# Uncomment to use shared PostgreSQL instead:
# POSTGRES_URI=postgresql://axuser:axpassword@host.docker.internal:5435/artefx

# ============================================================================
# Shared Infrastructure - Redis
# ============================================================================
REDIS_URL=redis://host.docker.internal
REDIS_PORT=6380

# ============================================================================
# Other vars (copy from your .env if needed)
# ============================================================================
# SENDGRID_MOCK_API_KEY=your-key
