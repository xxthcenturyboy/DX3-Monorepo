import { HEALTHZ_STATUS_ERROR, HEALTHZ_STATUS_OK } from './healthz-api.const'

describe('HEALTHZ_STATUS_ERROR', () => {
  it('should be defined when imported', () => {
    // arrange
    // act
    // assert
    expect(HEALTHZ_STATUS_ERROR).toBeDefined()
  })
})

describe('HEALTHZ_STATUS_OK', () => {
  it('should be defined when imported', () => {
    // arrange
    // act
    // assert
    expect(HEALTHZ_STATUS_OK).toBeDefined()
  })
})
