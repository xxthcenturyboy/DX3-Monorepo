import { FADE_TIMEOUT_DUR, MEDIA_BREAK } from './ui.consts'

describe('FADE_TIMEOUT_DUR', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(FADE_TIMEOUT_DUR).toBeDefined()
  })
})

describe('MEDIA_BREAK', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(MEDIA_BREAK).toBeDefined()
  })
})
