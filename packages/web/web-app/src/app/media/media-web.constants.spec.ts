import { MEDIA_ENTITY_NAME } from './media-web.constants'

describe('media-web.constants', () => {
  describe('MEDIA_ENTITY_NAME', () => {
    it('should be "media"', () => {
      expect(MEDIA_ENTITY_NAME).toBe('media')
    })
  })
})
