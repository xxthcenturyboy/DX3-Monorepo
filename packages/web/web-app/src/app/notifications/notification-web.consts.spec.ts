import { NOTIFICATION_WEB_ENTITY_NAME } from './notification-web.consts'

describe('notification-web.consts', () => {
  describe('NOTIFICATION_WEB_ENTITY_NAME', () => {
    it('should be "notifications"', () => {
      expect(NOTIFICATION_WEB_ENTITY_NAME).toBe('notifications')
    })
  })
})
