import { SHORTLINK_WEB_ENTITY_NAME } from './shortlink-web.consts'

describe('SHORTLINK_ESHORTLINK_WEB_ENTITY_NAMENTITY_NAME ', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(SHORTLINK_WEB_ENTITY_NAME).toBeDefined()
  })

  it('should have correct value', () => {
    // arrange
    // act
    // assert
    expect(SHORTLINK_WEB_ENTITY_NAME).toEqual('shortlink')
  })
})
