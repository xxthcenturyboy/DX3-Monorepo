import { dialogOverridesDark } from './dialog-dark'

describe('dialogOverridesDark', () => {
  it('should be defined', () => {
    expect(dialogOverridesDark).toBeDefined()
  })

  it('should be an object', () => {
    expect(typeof dialogOverridesDark).toBe('object')
  })
})
