import { dialogOverridesLight } from './dialog-light'

describe('dialogOverridesLight', () => {
  it('should be defined', () => {
    expect(dialogOverridesLight).toBeDefined()
  })

  it('should be an object', () => {
    expect(typeof dialogOverridesLight).toBe('object')
  })
})
