import { UI_WEB_ENTITY_NAME } from './ui-web.consts'

describe('UI_WEB_ENTITY_NAME', () => {
  it('should exist when imported', () => {
    // arrange
    // act
    // assert
    expect(UI_WEB_ENTITY_NAME).toBeDefined()
  })
})
